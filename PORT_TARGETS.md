# Port targets — `color-eyre-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `color-eyre` that codex-rs uses, and therefore the minimum subset that `color-eyre-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-32] (rank `270`, tier `direct`, status `To Do`)

**Cargo package name**: `color-eyre`
**Rust module name (in `use` statements)**: `color_eyre`

## Summary

- **5** distinct symbols imported by codex-rs
- **16** total `use` statements
- **11** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `color_eyre::eyre::Result`

- **8 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/app.rs:152` (`codex-tui`) — `use color_eyre::eyre::Result`
- `tui/src/app/resize_reflow.rs:22` (`codex-tui`) — `use color_eyre::eyre::Result`
- `tui/src/app_backtrack.rs:43` (`codex-tui`) — `use color_eyre::eyre::Result`
- `tui/src/app_server_session.rs:118` (`codex-tui`) — `use color_eyre::eyre::Result`
- `tui/src/external_agent_config_migration_startup.rs:13` (`codex-tui`) — `use color_eyre::eyre::Result`
- `tui/src/external_editor.rs:6` (`codex-tui`) — `use color_eyre::eyre::Result`
- `tui/src/onboarding/onboarding_screen.rs:49` (`codex-tui`) — `use color_eyre::eyre::Result`
- `tui/src/resume_picker.rs:24` (`codex-tui`) — `use color_eyre::eyre::Result`

### `color_eyre::eyre::WrapErr`

- **4 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/app.rs:153` (`codex-tui`) — `use color_eyre::eyre::WrapErr`
- `tui/src/app_server_session.rs:119` (`codex-tui`) — `use color_eyre::eyre::WrapErr`
- `tui/src/external_agent_config_migration_startup.rs:14` (`codex-tui`) — `use color_eyre::eyre::WrapErr`
- `tui/src/lib.rs:60` (`codex-tui`) — `use color_eyre::eyre::WrapErr`

### `color_eyre::Result`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/cwd_prompt.rs:12` (`codex-tui`) — `use color_eyre::Result`
- `tui/src/update_prompt.rs:16` (`codex-tui`) — `use color_eyre::Result`

### `color_eyre::eyre::Report`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/external_editor.rs:4` (`codex-tui`) — `use color_eyre::eyre::Report`

### `color_eyre::eyre::ContextCompat`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/app_server_session.rs:117` (`codex-tui`) — `use color_eyre::eyre::ContextCompat`
